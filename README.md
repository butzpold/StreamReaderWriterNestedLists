StreamReaderWriterNestedLists

The Program (c#, .Net 9.0, VS22) creates a NestedList.csv. and write some lines/rows into it. Afterwards it reads out the rows, change some values (add 5 to each value of certain "column") and append the rows with the changed values.
